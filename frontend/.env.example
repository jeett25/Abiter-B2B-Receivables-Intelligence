# Base URL of the FastAPI backend (backend/app/main.py).
# Local dev default matches `uvicorn app.main:app --reload` (port 8000).
# Point this at the deployed Render URL only when recording the demo video --
# see CLAUDE.md's "IMPORTANT -- REMIND USER BEFORE ANY DEMO RECORDING SESSION".
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
